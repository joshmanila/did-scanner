DATABASE_URL=
APP_ENCRYPTION_KEY=
CONVOSO_USE_FIXTURES=
VERCEL_CRON_SECRET=

# Optional legacy fallback: one-time seeding of a "Default" dialer
# through `npm run db:seed` when the dialers table is empty.
CONVOSO_API_URL=
CONVOSO_AUTH_TOKEN=
